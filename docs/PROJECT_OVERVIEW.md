# Project Overview

This project packages a lightweight manufacturing defect classification workflow.

## Core idea

- Train on NEU surface defect images
- Export a reusable model
- Run single-image inference
- Open a web interface for desktop and mobile browsers

## Why this project matters

A lightweight browser-based workflow can reduce the friction of running image-based quality checks without requiring a native mobile application.
